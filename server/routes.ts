import { Router, Request, Response, NextFunction } from 'express';
import crypto from 'crypto';
import { supabase, verifyPassword, hashPassword } from './db.js';
import { calculateMatchPoints, computeLeaderboard } from './scoring.js';

export const apiRouter = Router();
const fail = (res: Response, error: any, status = 500) => res.status(status).json({ error: error?.message || String(error) });
const audit = async (action: string, admin_user: string, details: any) => { const { error } = await supabase.from('audit_logs').insert({ id: `audit_${Date.now()}_${Math.random().toString(36).slice(2, 7)}`, action, admin_user, details: typeof details === 'string' ? details : JSON.stringify(details), created_at: new Date().toISOString() }); if (error) console.error('Failed to write audit log:', error); };
const settings = async () => { const result = await supabase.from('tournament_settings').select('key,value'); if (result.error) throw result.error; const values: Record<string, any> = {}; for (const row of result.data || []) { try { values[row.key] = JSON.parse(row.value); } catch { values[row.key] = row.value; } } return values; };
const count = async (table: string, filter?: (query: any) => any) => { let query = supabase.from(table).select('*', { count: 'exact', head: true }); if (filter) query = filter(query); const result = await query; if (result.error) throw result.error; return result.count || 0; };
const authToken = (req: Request) => req.headers.authorization?.startsWith('Bearer ') ? req.headers.authorization.substring(7).trim() : req.cookies?.admin_session;
const relation = <T>(value: T | T[] | null | undefined): T | undefined => Array.isArray(value) ? value[0] : value || undefined;

export async function requireAdmin(req: Request, res: Response, next: NextFunction): Promise<void> { try { const token = authToken(req); if (!token) { res.status(401).json({ error: 'Unauthorized: Admin authentication token required' }); return; } const result = await supabase.from('admin_sessions').select('token,expires_at').eq('token', token).maybeSingle(); if (result.error) throw result.error; if (!result.data) { res.status(401).json({ error: 'Unauthorized: Invalid or expired session' }); return; } if (Date.now() > Number(result.data.expires_at)) { await supabase.from('admin_sessions').delete().eq('token', token); res.status(401).json({ error: 'Unauthorized: Session expired, please login again' }); return; } (req as any).adminToken = token; next(); } catch (error) { fail(res, error); } }

apiRouter.post('/admin/login', async (req, res) => { try { const ip = (req.headers['x-forwarded-for'] as string)?.split(',')[0].trim() || req.ip || '127.0.0.1'; const attempt = await supabase.from('login_attempts').select('attempts,last_attempt').eq('ip', ip).maybeSingle(); if (attempt.error) throw attempt.error; if (attempt.data && attempt.data.attempts >= 5 && Date.now() - Number(attempt.data.last_attempt) < 600000) { res.status(429).json({ error: `Too many failed login attempts. Please wait ${Math.ceil((600000 - Date.now() + Number(attempt.data.last_attempt)) / 1000)} seconds before trying again.` }); return; } const { password } = req.body; if (!password || typeof password !== 'string') { res.status(400).json({ error: 'Password is required' }); return; } const record = await supabase.from('admin_auth').select('password_hash,salt').eq('id', 'admin_root').maybeSingle(); if (record.error) throw record.error; if (!record.data) { res.status(500).json({ error: 'Server authentication uninitialized' }); return; } const valid = verifyPassword(password, record.data.password_hash, record.data.salt); if (!valid) { await supabase.from('login_attempts').upsert({ ip, attempts: (attempt.data?.attempts || 0) + 1, last_attempt: Date.now() }); res.status(401).json({ error: 'Invalid admin password' }); return; } await supabase.from('login_attempts').delete().eq('ip', ip); const token = crypto.randomBytes(32).toString('hex'); const expiresAt = Date.now() + 86400000; const session = await supabase.from('admin_sessions').insert({ token, created_at: new Date().toISOString(), expires_at: expiresAt }); if (session.error) throw session.error; await audit('ADMIN_LOGIN_SUCCESS', 'admin', { ip }); res.cookie('admin_session', token, { httpOnly: true, secure: process.env.NODE_ENV === 'production', sameSite: 'lax', maxAge: 86400000 }); res.json({ success: true, token, expiresAt }); } catch (error) { fail(res, error); } });
apiRouter.post('/admin/logout', async (req, res) => { try { const token = authToken(req); if (token) { const result = await supabase.from('admin_sessions').delete().eq('token', token); if (result.error) throw result.error; } res.clearCookie('admin_session'); res.json({ success: true, message: 'Logged out successfully' }); } catch (error) { fail(res, error); } });
apiRouter.get('/admin/me', requireAdmin, (_req, res) => res.json({ authenticated: true, user: 'admin' }));
apiRouter.post('/admin/change-password', requireAdmin, async (req, res) => { try { const { currentPassword, newPassword } = req.body; if (!newPassword || typeof newPassword !== 'string' || newPassword.length < 6) { res.status(400).json({ error: 'New password must be at least 6 characters long' }); return; } const record = await supabase.from('admin_auth').select('password_hash,salt').eq('id', 'admin_root').single(); if (record.error) throw record.error; if (!verifyPassword(currentPassword, record.data.password_hash, record.data.salt)) { res.status(400).json({ error: 'Current password does not match' }); return; } const next = hashPassword(newPassword); const result = await supabase.from('admin_auth').update({ password_hash: next.hash, salt: next.salt, updated_at: new Date().toISOString() }).eq('id', 'admin_root'); if (result.error) throw result.error; await audit('PASSWORD_CHANGED', 'admin', 'Admin password was updated successfully'); res.json({ success: true, message: 'Password updated successfully' }); } catch (error) { fail(res, error); } });

apiRouter.get('/settings', async (_req, res) => { try { res.json({ settings: await settings() }); } catch (error) { fail(res, error); } });
apiRouter.get('/tournament', async (_req, res) => { try { const tournamentSettings = await settings(); const [totalTeams, totalMatches, completedMatches, resultRows] = await Promise.all([count('teams'), count('matches'), count('matches', q => q.in('status', ['Completed', 'Locked'])), supabase.from('match_results').select('kills,placement')]); if (resultRows.error) throw resultRows.error; const results = resultRows.data || []; const next = await supabase.from('matches').select('*').eq('status', 'Scheduled').order('match_number').limit(1).maybeSingle(); const recent = await supabase.from('matches').select('*').in('status', ['Completed', 'Locked']).order('match_number', { ascending: false }).limit(1).maybeSingle(); res.json({ settings: tournamentSettings, stats: { totalTeams, completedMatches, totalMatches, totalKills: results.reduce((sum, row) => sum + row.kills, 0), totalChickenDinners: results.filter(row => row.placement === 1).length, remainingMatches: Math.max(0, totalMatches - completedMatches) }, nextMatch: next.data, recentMatch: recent.data }); } catch (error) { fail(res, error); } });
apiRouter.get('/leaderboard', async (req, res) => { try { res.json(await computeLeaderboard((req.query.stage as 'group' | 'finals') || 'group', (req.query.groupId as string) || ((req.query.stage || 'group') === 'group' ? 'grp_a' : undefined))); } catch (error) { fail(res, error); } });
apiRouter.get('/groups', async (_req, res) => { try { const [groups, teams] = await Promise.all([supabase.from('groups').select('*').order('display_order'), supabase.from('teams').select('id,name,tag,logo_url,group_id,status').order('name')]); if (groups.error) throw groups.error; if (teams.error) throw teams.error; res.json({ groups: (groups.data || []).map(group => ({ ...group, teams: (teams.data || []).filter(team => team.group_id === group.id) })), unassigned: (teams.data || []).filter(team => !team.group_id) }); } catch (error) { fail(res, error); } });

apiRouter.get('/matches', async (req, res) => { try { let query = supabase.from('matches').select('*, groups(name)').order('match_number'); if (req.query.stage) query = query.eq('stage', req.query.stage); if (req.query.groupId) query = query.eq('group_id', req.query.groupId); const response = await query; if (response.error) throw response.error; const matches = await Promise.all((response.data || []).map(async match => { const normalized = { ...match, group_name: match.groups?.name }; delete (normalized as any).groups; if (!['Completed', 'Locked'].includes(match.status)) return normalized; const [winner, topKill] = await Promise.all([supabase.from('match_results').select('placement,kills,total_points,teams(name,tag,logo_url)').eq('match_id', match.id).eq('placement', 1).maybeSingle(), supabase.from('match_results').select('kills,teams(name,tag)').eq('match_id', match.id).order('kills', { ascending: false }).limit(1).maybeSingle()]); const winnerTeam = relation(winner.data?.teams); const topKillTeam = relation(topKill.data?.teams); return { ...normalized, winner: winner.data ? { ...winner.data, team_name: winnerTeam?.name, team_tag: winnerTeam?.tag, logo_url: winnerTeam?.logo_url } : null, topKillTeam: topKill.data ? { kills: topKill.data.kills, team_name: topKillTeam?.name, team_tag: topKillTeam?.tag } : null }; })); res.json({ matches }); } catch (error) { fail(res, error); } });
apiRouter.get('/matches/:id', async (req, res) => { try { const match = await supabase.from('matches').select('*, groups(name)').eq('id', req.params.id).maybeSingle(); if (match.error) throw match.error; if (!match.data) { res.status(404).json({ error: 'Match not found' }); return; } const normalizedMatch = { ...match.data, group_name: match.data.groups?.name }; delete (normalizedMatch as any).groups; const results = await supabase.from('match_results').select('*, teams(name,tag,logo_url)').eq('match_id', req.params.id).order('placement'); if (results.error) throw results.error; const normalized = (results.data || []).map(row => ({ ...row, team_name: row.teams?.name, team_tag: row.teams?.tag, team_logo: row.teams?.logo_url })); res.json({ match: normalizedMatch, results: normalized, stats: { totalMatchKills: normalized.reduce((sum, row) => sum + row.kills, 0), highestScore: Math.max(0, ...normalized.map(row => row.total_points)), highestKills: Math.max(0, ...normalized.map(row => row.kills)), participatingTeamsCount: normalized.length }, winner: normalized.find(row => row.placement === 1) || null }); } catch (error) { fail(res, error); } });

apiRouter.get('/teams', async (req, res) => { try { let query = supabase.from('teams').select('*, groups(name)').order('name'); if (req.query.search) query = query.or(`name.ilike.%${req.query.search}%,tag.ilike.%${req.query.search}%`); if (req.query.groupId) query = query.eq('group_id', req.query.groupId); const [teams, players] = await Promise.all([query, supabase.from('players').select('*').order('role')]); if (teams.error) throw teams.error; if (players.error) throw players.error; res.json({ teams: (teams.data || []).map(team => { const normalized = { ...team, group_name: team.groups?.name, players: (players.data || []).filter(player => player.team_id === team.id) }; delete (normalized as any).groups; return normalized; }) }); } catch (error) { fail(res, error); } });
apiRouter.get('/teams/:id', async (req, res) => { try { const team = await supabase.from('teams').select('*, groups(name)').or(`id.eq.${req.params.id},tag.ilike.${req.params.id},name.ilike.${req.params.id}`).limit(1).maybeSingle(); if (team.error) throw team.error; if (!team.data) { res.status(404).json({ error: 'Team not found' }); return; } const [players, history] = await Promise.all([supabase.from('players').select('*').eq('team_id', team.data.id), supabase.from('match_results').select('*, matches(name,match_number,map,stage,status)').eq('team_id', team.data.id).order('match_id')]); if (players.error) throw players.error; if (history.error) throw history.error; const matchHistory = (history.data || []).map(row => ({ ...row, match_name: row.matches?.name, match_number: row.matches?.match_number, map: row.matches?.map, stage: row.matches?.stage, match_status: row.matches?.status })); const normalizedTeam = { ...team.data, group_name: team.data.groups?.name }; delete (normalizedTeam as any).groups; res.json({ team: normalizedTeam, players: players.data || [], matchHistory, stats: { matchesPlayed: matchHistory.length, totalKills: matchHistory.reduce((sum, row) => sum + row.kills, 0), totalPoints: matchHistory.reduce((sum, row) => sum + row.total_points, 0), bestPlacement: matchHistory.length ? Math.min(...matchHistory.map(row => row.placement)) : 0, chickenDinners: matchHistory.filter(row => row.placement === 1).length } }); } catch (error) { fail(res, error); } });
apiRouter.get('/finals', async (_req, res) => { try { const leaderboard = await computeLeaderboard('finals'); const [matches, teams] = await Promise.all([supabase.from('matches').select('*').eq('stage', 'finals').order('match_number'), supabase.from('teams').select('*, groups(name)').eq('status', 'Qualified')]); if (matches.error) throw matches.error; if (teams.error) throw teams.error; const finalsMatches = matches.data || []; const isFinalsCompleted = finalsMatches.length > 0 && finalsMatches.every(match => ['Completed', 'Locked'].includes(match.status)); res.json({ leaderboard: leaderboard.leaderboard, matches: finalsMatches, qualifiedTeams: (teams.data || []).map(team => ({ ...team, group_name: team.groups?.name })), isFinalsCompleted, champion: isFinalsCompleted ? leaderboard.leaderboard[0] || null : null }); } catch (error) { fail(res, error); } });
apiRouter.get('/rules', async (_req, res) => { try { const result = await supabase.from('tournament_rules').select('*').order('display_order'); if (result.error) throw result.error; res.json({ rules: result.data || [] }); } catch (error) { fail(res, error); } });

apiRouter.get('/admin/dashboard', requireAdmin, async (_req, res) => { try { const [totalTeams, groupA, groupB, completed, totalMatches, resultRows, qualified, status, leaders, logs] = await Promise.all([count('teams'), count('teams', q => q.eq('group_id', 'grp_a')), count('teams', q => q.eq('group_id', 'grp_b')), count('matches', q => q.in('status', ['Completed', 'Locked'])), count('matches'), supabase.from('match_results').select('kills'), count('teams', q => q.eq('status', 'Qualified')), supabase.from('tournament_settings').select('value').eq('key', 'status').maybeSingle(), Promise.all([computeLeaderboard('group', 'grp_a'), computeLeaderboard('group', 'grp_b')]), supabase.from('audit_logs').select('*').order('created_at', { ascending: false }).limit(15)]); if (resultRows.error || status.error || logs.error) throw resultRows.error || status.error || logs.error; res.json({ stats: { totalTeams, groupATeams: groupA, groupBTeams: groupB, matchesCompleted: completed, matchesRemaining: Math.max(0, totalMatches - completed), totalKills: (resultRows.data || []).reduce((sum, row) => sum + row.kills, 0), qualifiedCount: qualified, tournamentStatus: status.data?.value || 'Group Stage' }, leaders: { groupA: leaders[0].leaderboard[0] || null, groupB: leaders[1].leaderboard[0] || null }, auditLogs: logs.data || [] }); } catch (error) { fail(res, error); } });

apiRouter.post('/admin/teams', requireAdmin, async (req, res) => { try { const { name, tag, logo_url, group_id, status } = req.body; if (!name || !tag) { res.status(400).json({ error: 'Team name and tag are required' }); return; } const id = `team_${Date.now()}_${Math.random().toString(36).slice(2, 6)}`; const team = { id, name: name.trim(), tag: tag.trim().toUpperCase(), logo_url: logo_url || '', group_id: group_id || null, status: status || 'Registered', created_at: new Date().toISOString() }; const result = await supabase.from('teams').insert(team); if (result.error) return fail(res, result.error, 400); await audit('TEAM_CREATED', 'admin', { teamId: id, name, tag }); res.json({ success: true, team }); } catch (error) { fail(res, error, 400); } });
apiRouter.put('/admin/teams/:id', requireAdmin, async (req, res) => { try { const { name, tag, logo_url, group_id, status } = req.body; const result = await supabase.from('teams').update({ ...(name !== undefined && { name }), ...(tag !== undefined && { tag }), ...(logo_url !== undefined && { logo_url }), ...(group_id !== undefined && { group_id }), ...(status !== undefined && { status }) }).eq('id', req.params.id); if (result.error) return fail(res, result.error, 400); await audit('TEAM_UPDATED', 'admin', { teamId: req.params.id, name, tag, status }); res.json({ success: true, message: 'Team updated' }); } catch (error) { fail(res, error, 400); } });
apiRouter.delete('/admin/teams/:id', requireAdmin, async (req, res) => { try { const result = await supabase.from('teams').delete().eq('id', req.params.id); if (result.error) throw result.error; await audit('TEAM_DELETED', 'admin', { teamId: req.params.id }); res.json({ success: true, message: 'Team deleted' }); } catch (error) { fail(res, error); } });
apiRouter.post('/admin/teams/:id/players', requireAdmin, async (req, res) => { try { const { name, in_game_name, in_game_id, role } = req.body; if (!name || !in_game_name) { res.status(400).json({ error: 'Player real name and in-game name are required' }); return; } const playerId = `p_${Date.now()}_${Math.random().toString(36).slice(2, 6)}`; const result = await supabase.from('players').insert({ id: playerId, team_id: req.params.id, name: name.trim(), in_game_name: in_game_name.trim(), in_game_id: in_game_id || '', role: role || 'Assaulter' }); if (result.error) throw result.error; await audit('PLAYER_ADDED', 'admin', { teamId: req.params.id, playerId, in_game_name }); res.json({ success: true, playerId }); } catch (error) { fail(res, error); } });
apiRouter.delete('/admin/players/:id', requireAdmin, async (req, res) => { try { const result = await supabase.from('players').delete().eq('id', req.params.id); if (result.error) throw result.error; res.json({ success: true, message: 'Player removed' }); } catch (error) { fail(res, error); } });

const matchPayload = (body: any, id: string) => ({ id, match_number: body.match_number || 1, name: body.name || `Match ${body.match_number || 1}`, stage: body.stage || 'group', group_id: body.group_id || null, map: body.map || 'Erangel', status: 'Scheduled', scheduled_time: body.scheduled_time || '', is_locked: false, created_at: new Date().toISOString() });
apiRouter.post('/admin/matches', requireAdmin, async (req, res) => { try { const id = `m_${Date.now()}_${Math.random().toString(36).slice(2, 6)}`; const result = await supabase.from('matches').insert(matchPayload(req.body, id)); if (result.error) throw result.error; await audit('MATCH_CREATED', 'admin', { matchId: id, name: req.body.name, map: req.body.map }); res.json({ success: true, matchId: id }); } catch (error) { fail(res, error); } });
apiRouter.put('/admin/matches/:id', requireAdmin, async (req, res) => { try { const existing = await supabase.from('matches').select('is_locked').eq('id', req.params.id).maybeSingle(); if (existing.error) throw existing.error; if (existing.data?.is_locked) { res.status(400).json({ error: 'This match is LOCKED. Please unlock the match before editing details.' }); return; } const body = req.body; const result = await supabase.from('matches').update(Object.fromEntries(Object.entries({ name: body.name, match_number: body.match_number, stage: body.stage, group_id: body.group_id, map: body.map, status: body.status, scheduled_time: body.scheduled_time }).filter(([, value]) => value !== undefined))).eq('id', req.params.id); if (result.error) throw result.error; await audit('MATCH_UPDATED', 'admin', { matchId: req.params.id, name: body.name, status: body.status }); res.json({ success: true, message: 'Match updated' }); } catch (error) { fail(res, error); } });
apiRouter.delete('/admin/matches/:id', requireAdmin, async (req, res) => { try { const existing = await supabase.from('matches').select('is_locked').eq('id', req.params.id).maybeSingle(); if (existing.data?.is_locked) { res.status(400).json({ error: 'Cannot delete a locked match. Unlock first with admin confirmation.' }); return; } const result = await supabase.from('matches').delete().eq('id', req.params.id); if (result.error) throw result.error; await audit('MATCH_DELETED', 'admin', { matchId: req.params.id }); res.json({ success: true, message: 'Match deleted' }); } catch (error) { fail(res, error); } });
apiRouter.post('/admin/matches/:id/lock', requireAdmin, async (req, res) => { try { const result = await supabase.from('matches').update({ is_locked: true, status: 'Locked', locked_at: new Date().toISOString() }).eq('id', req.params.id); if (result.error) throw result.error; await audit('MATCH_LOCKED', 'admin', { matchId: req.params.id }); res.json({ success: true, message: 'Match verified and locked' }); } catch (error) { fail(res, error); } });
apiRouter.post('/admin/matches/:id/unlock', requireAdmin, async (req, res) => { try { if (!req.body.reason) { res.status(400).json({ error: 'An audit reason is required to unlock a verified match' }); return; } const result = await supabase.from('matches').update({ is_locked: false, status: 'Completed' }).eq('id', req.params.id); if (result.error) throw result.error; await audit('MATCH_UNLOCKED', 'admin', { matchId: req.params.id, reason: req.body.reason }); res.json({ success: true, message: 'Match unlocked' }); } catch (error) { fail(res, error); } });

apiRouter.post('/admin/matches/:id/results', requireAdmin, async (req, res) => { try { const match = await supabase.from('matches').select('*').eq('id', req.params.id).maybeSingle(); if (match.error) throw match.error; if (!match.data) { res.status(404).json({ error: 'Match not found' }); return; } if (match.data.is_locked) { res.status(400).json({ error: 'Match is locked. Cannot enter or overwrite results.' }); return; } const { entries, finalizeAndLock } = req.body; if (!Array.isArray(entries) || !entries.length) { res.status(400).json({ error: 'Entries array is required' }); return; } const teams = new Set<string>(), placements = new Set<number>(), rows = []; for (const entry of entries) { if (!entry.team_id || typeof entry.placement !== 'number' || typeof entry.kills !== 'number') { res.status(400).json({ error: 'Each entry must have team_id, placement, and kills' }); return; } if (entry.placement < 1 || entry.kills < 0 || teams.has(entry.team_id) || placements.has(entry.placement)) { res.status(400).json({ error: 'Invalid or duplicate result entry' }); return; } teams.add(entry.team_id); placements.add(entry.placement); const points = await calculateMatchPoints(entry.placement, entry.kills); rows.push({ id: `res_${Date.now()}_${Math.random().toString(36).slice(2, 7)}`, match_id: req.params.id, team_id: entry.team_id, placement: entry.placement, kills: entry.kills, ...points, created_at: new Date().toISOString(), updated_at: new Date().toISOString() }); } const deleted = await supabase.from('match_results').delete().eq('match_id', req.params.id); if (deleted.error) throw deleted.error; const inserted = await supabase.from('match_results').insert(rows); if (inserted.error) throw inserted.error; const updated = await supabase.from('matches').update(finalizeAndLock ? { status: 'Locked', is_locked: true, locked_at: new Date().toISOString() } : { status: 'Completed' }).eq('id', req.params.id); if (updated.error) throw updated.error; await audit(finalizeAndLock ? 'RESULTS_FINALIZED_AND_LOCKED' : 'RESULTS_SAVED', 'admin', { matchId: req.params.id, totalEntries: rows.length }); res.json({ success: true, message: 'Results saved and authoritative scoring computed', count: rows.length }); } catch (error) { fail(res, error); } });

apiRouter.post('/admin/matches/:id/results/csv-preview', requireAdmin, async (req, res) => { try { const match = await supabase.from('matches').select('*').eq('id', req.params.id).maybeSingle(); if (match.error) throw match.error; if (!match.data) { res.status(404).json({ error: 'Match not found' }); return; } if (!req.body.csvText || typeof req.body.csvText !== 'string') { res.status(400).json({ error: 'CSV text content is required' }); return; } const teams = await supabase.from('teams').select('id,name,tag,group_id'); if (teams.error) throw teams.error; const lines = req.body.csvText.split(/\r?\n/).map((line: string) => line.trim()).filter(Boolean); const parsedRows: any[] = [], errors: string[] = [], warnings: string[] = [], seenTeams = new Set<string>(), seenPlacements = new Set<number>(); for (let i = lines[0]?.toLowerCase().includes('team') ? 1 : 0; i < lines.length; i++) { const parts = lines[i].split(',').map((part: string) => part.trim()), placement = Number.parseInt(parts[1], 10), kills = Number.parseInt(parts[2], 10), team = (teams.data || []).find(t => t.name.toLowerCase() === parts[0].toLowerCase() || t.tag.toLowerCase() === parts[0].toLowerCase() || t.id.toLowerCase() === parts[0].toLowerCase()); if (parts.length < 3 || !team || !Number.isInteger(placement) || placement < 1 || !Number.isInteger(kills) || kills < 0) { errors.push(`Row ${i + 1}: Invalid team, placement, or kills`); continue; } if (seenTeams.has(team.id) || seenPlacements.has(placement)) errors.push(`Row ${i + 1}: Duplicate team or placement`); seenTeams.add(team.id); seenPlacements.add(placement); if (match.data.group_id && team.group_id && match.data.group_id !== team.group_id) warnings.push(`Row ${i + 1}: Team "${team.name}" is assigned to another group.`); const points = await calculateMatchPoints(placement, kills); parsedRows.push({ team_id: team.id, team_name: team.name, team_tag: team.tag, placement, kills, ...points }); } res.json({ valid: errors.length === 0, parsedRows: parsedRows.sort((a, b) => a.placement - b.placement), errors, warnings, totalRows: parsedRows.length }); } catch (error) { fail(res, error); } });

apiRouter.post('/admin/finals/generate', requireAdmin, async (req, res) => { try { const qCount = Number(req.body.qualifiersPerGroup) || 8, mCount = Number(req.body.matchesCount) || 5; const [a, b] = await Promise.all([computeLeaderboard('group', 'grp_a'), computeLeaderboard('group', 'grp_b')]); const finalists = [...a.leaderboard.slice(0, qCount), ...b.leaderboard.slice(0, qCount)]; if (!finalists.length) { res.status(400).json({ error: 'Not enough group stage data to determine qualifiers' }); return; } const eliminated = await supabase.from('teams').update({ status: 'Eliminated' }).neq('id', ''); if (eliminated.error) throw eliminated.error; const qualified = await supabase.from('teams').update({ status: 'Qualified' }).in('id', finalists.map(team => team.team_id)); if (qualified.error) throw qualified.error; const removed = await supabase.from('matches').delete().eq('stage', 'finals'); if (removed.error) throw removed.error; const maps = ['Erangel', 'Miramar', 'Sanhok', 'Rondo', 'Erangel', 'Vikendi'], now = new Date().toISOString(); const matches = Array.from({ length: mCount }, (_, index) => ({ id: `m_finals_${index + 1}`, match_number: index + 1, name: `Grand Finals — Match ${index + 1}`, stage: 'finals', group_id: null, map: maps[index % maps.length], status: 'Scheduled', scheduled_time: `2026-09-12 ${14 + Math.floor(index * 0.75)}:${(index + 1) % 2 === 0 ? '30' : '00'} IST`, is_locked: false, created_at: now })); const inserted = await supabase.from('matches').insert(matches); if (inserted.error) throw inserted.error; const status = await supabase.from('tournament_settings').update({ value: 'Finals' }).eq('key', 'status'); if (status.error) throw status.error; await audit('FINALS_GENERATED', 'admin', { qualifiersA: Math.min(a.leaderboard.length, qCount), qualifiersB: Math.min(b.leaderboard.length, qCount), totalFinalists: finalists.length, finalsMatchesCreated: mCount }); res.json({ success: true, message: 'Finals squads successfully generated!', finalists: finalists.map(team => ({ id: team.team_id, name: team.team_name, tag: team.team_tag })), matchesCreated: mCount }); } catch (error) { fail(res, error); } });
apiRouter.put('/admin/settings', requireAdmin, async (req, res) => { try { const updates = Object.entries(req.body as Record<string, any>).map(([key, value]) => ({ key, value: typeof value === 'string' ? value : JSON.stringify(value) })); const result = await supabase.from('tournament_settings').upsert(updates, { onConflict: 'key' }); if (result.error) throw result.error; await audit('SETTINGS_UPDATED', 'admin', req.body); res.json({ success: true, message: 'Settings updated successfully' }); } catch (error) { fail(res, error); } });
apiRouter.post('/admin/rules', requireAdmin, async (req, res) => { try { const { category, title, content, display_order } = req.body; if (!title || !content) { res.status(400).json({ error: 'Title and content are required' }); return; } const id = `rule_${Date.now()}`; const result = await supabase.from('tournament_rules').insert({ id, category: category || 'General', title, content, display_order: display_order || 0 }); if (result.error) throw result.error; await audit('RULE_CREATED', 'admin', { id, title }); res.json({ success: true, id }); } catch (error) { fail(res, error); } });
apiRouter.put('/admin/rules/:id', requireAdmin, async (req, res) => { try { const body = req.body; const result = await supabase.from('tournament_rules').update(Object.fromEntries(Object.entries({ category: body.category, title: body.title, content: body.content, display_order: body.display_order }).filter(([, value]) => value !== undefined))).eq('id', req.params.id); if (result.error) throw result.error; await audit('RULE_UPDATED', 'admin', { id: req.params.id, title: body.title }); res.json({ success: true, message: 'Rule updated' }); } catch (error) { fail(res, error); } });
apiRouter.delete('/admin/rules/:id', requireAdmin, async (req, res) => { try { const result = await supabase.from('tournament_rules').delete().eq('id', req.params.id); if (result.error) throw result.error; await audit('RULE_DELETED', 'admin', { id: req.params.id }); res.json({ success: true, message: 'Rule deleted' }); } catch (error) { fail(res, error); } });
apiRouter.get('/admin/audit-logs', requireAdmin, async (_req, res) => { try { const result = await supabase.from('audit_logs').select('*').order('created_at', { ascending: false }).limit(50); if (result.error) throw result.error; res.json({ logs: result.data || [] }); } catch (error) { fail(res, error); } });
